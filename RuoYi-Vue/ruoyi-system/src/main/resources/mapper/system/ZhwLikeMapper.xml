<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ZhwLikeMapper">

    <resultMap id="LikeResult" type="ZhwLike">
        <id     property="likeId"     column="like_id"/>
        <result property="targetId"   column="target_id"/>
        <result property="targetType" column="target_type"/>
        <result property="userId"     column="user_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectLike" resultMap="LikeResult">
        SELECT like_id, target_id, target_type, user_id, create_time
        FROM zhw_like
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType}
        LIMIT 1
    </select>

    <insert id="insertLike" parameterType="ZhwLike" useGeneratedKeys="true" keyProperty="likeId">
        INSERT INTO zhw_like (target_id, target_type, user_id, create_time)
        VALUES (#{targetId}, #{targetType}, #{userId}, NOW())
    </insert>

    <delete id="deleteLike">
        DELETE FROM zhw_like
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType}
    </delete>

</mapper>
