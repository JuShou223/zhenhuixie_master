项目已经有完整的 Docker 部署配置了，非常好。下面给你完整的部署流程：

---

部署步骤

第一步：本地打包后端 JAR

在你的 Mac 终端运行：

cd /Users/xuyiming/Desktop/zhenhuixie/RuoYi-Vue
mvn clean package -Dmaven.test.skip=true

打包成功后 JAR 在：ruoyi-admin/target/ruoyi-admin.jar

---

第二步：上传文件到服务器

# 上传 docker 配置目录（替换 your_server_ip 为你的服务器公网IP）

scp -r /Users/xuyiming/Desktop/zhenhuixie/docker/data root@your_server_ip:/var/

# 上传打包好的 JAR

scp /Users/xuyiming/Desktop/zhenhuixie/RuoYi-Vue/ruoyi-admin/target/ruoyi-admin.jar \
 root@your_server_ip:/var/data/java/ruoyi-admin.jar

# 上传 SQL 初始化文件

scp /Users/xuyiming/Desktop/zhenhuixie/RuoYi-Vue/sql/ry_20260321.sql \
 root@your_server_ip:/var/data/mysql/initdb/01_ry.sql
scp /Users/xuyiming/Desktop/zhenhuixie/RuoYi-Vue/sql/quartz.sql \
 root@your_server_ip:/var/data/mysql/initdb/02_quartz.sql
scp /Users/xuyiming/Desktop/zhenhuixie/RuoYi-Vue/sql/zhw_init.sql \
 root@your_server_ip:/var/data/mysql/initdb/03_zhw.sql

---

第三步：服务器上安装 Docker

SSH 登录服务器后执行：

# 安装 Docker

curl -fsSL https://get.docker.com | sh

# 启动 Docker 并设置开机自启

systemctl enable docker && systemctl start docker

# 安装 Docker Compose（V2）

apt-get install -y docker-compose-plugin

# 验证

docker --version
docker-compose version

---

第四步：启动所有服务

# 设置权限

chmod 777 -R /var/data

# 启动

cd /var/data
docker-compose up -d

---

第五步：查看状态

# 查看所有容器是否正常

docker-compose ps

# 查看后端日志（如果有问题）

docker-compose logs -f java

正常状态应该是 4 个容器都 running：redis、mysql、java、nginx

---

验证访问

┌───────────────────────────────────┬────────────────────────────┐
│ 地址 │ 说明 │
├───────────────────────────────────┼────────────────────────────┤
│ http://服务器IP/prod-api/actuator │ 后端是否存活 │
├───────────────────────────────────┼────────────────────────────┤
│ http://服务器IP │ Nginx 首页（此时还没前端） │
└───────────────────────────────────┴────────────────────────────┘

---

注意事项

1. 阿里云安全组 要开放端口：80、8080（可选）、3306（不建议公开）、6379（不建议公开）
2. SQL 初始化只在 MySQL 首次创建时执行，如果容器已存在需手动导入
3. 服务器内存建议 2G 以上，Spring Boot + MySQL + Redis 同时跑比较吃内存

---

你先看一下本地的 SQL 文件名是否正确，运行：

ls /Users/xuyiming/Desktop/zhenhuixie/RuoYi-Vue/sql/

确认文件名后我们开始第一步打包。
